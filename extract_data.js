const { execSync } = require('child_process'); const fs = require('fs'); const origLearn = execSync('git show HEAD:src/features/statistics/LearnSection.tsx').toString(); const origQuiz = execSync('git show HEAD:src/features/statistics/QuizSection.tsx').toString(); const learnLines = origLearn.split('\n'); const learnSlidesStr = learnLines.slice(5, 352).join('\n'); const learnOut = \import { LineChart, Line, PieChart, Pie, Cell, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts';\n\nexport \ + learnSlidesStr; fs.writeFileSync('src/features/statistics/data/learnSlides.tsx', learnOut); const quizLines = origQuiz.split('\n'); const quizQuestionsStr = quizLines.slice(6, 297).join('\n'); const quizOut = \import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Cell } from 'recharts';\n\nexport \ + quizQuestionsStr; fs.writeFileSync('src/features/statistics/data/quizQuestions.tsx', quizOut);
